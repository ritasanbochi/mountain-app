// mountains_extra.js
// 花100 / 二百名山 / 三百名山 を「百名山と同じ形式」で固定保持するためのファイル。
// まずは空でOK（エラー回避のため default export を必ず提供する）

const mountainsExtra = [
  // ここに後でデータを入れる
  // 例:
  // { name:"黒檜山", lat:36.5619, lng:139.1960, elev:1828, level:"中級", gpx:null, weather:{}, _sets:["HANA_100"] },
];

export default mountainsExtra;
